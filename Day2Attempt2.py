from itertools import chain

with open('Data.txt', 'r') as int:
    Data = int.read()


range = Data.split(",")[0].strip()
print(range, end="")
def parse_range(range_str):
     parts = range_str.strip().split('-')

for range in Data:
    len(range)  
    for range in chain(Data):       
        range is True
          
    else:
          continue
          print(" Potential Invalid ID:", range, end="")
         #if a value has an odd number of digits, it cannot be an invalid ID 



    









#if len(range) is 
    
#range(start,stop,step)
#--> for odd lengths of values
    

#range_1 = Data.split(",")[0].strip()
#    print(range_1)



    